spring.application.name=sos-backend

spring.datasource.url=jdbc:mysql://localhost:3306/sos_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=nicole

spring.jpa.hibernate.ddl-auto=validate
spring.jpa.open-in-view=false

spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration

spring.docker.compose.enabled=false

server.port=8080

jwt.secret=${JWT_SECRET:sos-exe-dev-secret-key-2026-change-me-very-long}
jwt.access-expiration-ms=900000
refresh-token.expiration-ms=604800000